post:
  summary: Đăng nhập hệ thống
  tags: [Auth]
  requestBody:
    content:
      application/json:
        schema:
          $ref: './schema_user.yaml#/LoginRequest'
  responses:
    '200':
      description: Đăng nhập thành công
      content:
        application/json:
          schema:
            $ref: './schema_user.yaml#/LoginResponse'
